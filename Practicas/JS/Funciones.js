
function saludar(nombre) {
    alert ("hola " + nombre);
}

function suma(numerouno,numerodos) {
    alert ('el resltado es ' + (numerouno+numerodos));
}

const constante = 10;

function comparacion (numero) {
    if (constante<numero) {
        alert ('el numero es mayor');
    }
    else {
        alert ('el numero es menor');
    }
}